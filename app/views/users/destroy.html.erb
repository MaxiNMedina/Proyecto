<div class="hsh-window container">
	<h1> Borrar Usuario </h1>

  <%= form_for(:user, :url => {:action => 'destroy', :id => @user.id}) do |f| %>
    <p>Borrar este usuario - Ok?</p>
    <%= submit_tag("Borrar") %>
  <% end %>

</div>