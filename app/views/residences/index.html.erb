<h1> Listado de Residencias </h1>  <!-- TABLA DE RESIDENCIAS-->

<table class="table">

	<tr>
		<th> Nombre </th>
		<th> Descripción </th>
	</tr>

	<% residences = Residence.all %>
	<% residences.each do |residence| %>
		<tr>
			<td> <%= link_to residence.nombre, residence %> </td>
			<td>
				<%= link_to "Editar", edit_residence_path(residence), class: 'btn btn-info btn-sm' %>
			</td>
			
		</tr>
	<% end %>

	<% if residences.size == 0 %>
		<tr> <td colspan="3"> No hay residencias </td> </tr>
	<% end %>

</table>
