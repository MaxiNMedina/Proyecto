<h1> Listado de Residencias </h1>  <!-- TABLA DE RESIDENCIAS-->

<table class="table">

	<tr>
		<th> Nombre </th>
		<th> Descripción </th>
	</tr>

	<% residences = Residence.all %>
	<% residences.each do |residence| %>
		<tr>
			<td> <%= link_to residence.nombre, residence %> </td>
			<!--
			<td> <%= residencia.descripcion %> </td>
			<td> <%= (monstruo.critter_star) ? 'Si' : 'No' %> </td>
			<td>
				<%= link_to "Editar", edit_monstruo_path(monstruo), class: 'btn btn-info btn-sm' %>
				<%= link_to "Eliminar", monstruo, method: :delete, class: 'btn btn-danger btn-sm' %>
			</td>
			-->
		</tr>
	<% end %>

	<% if residences.size == 0 %>
		<tr> <td colspan="3"> No hay residencias </td> </tr>
	<% end %>

</table>
